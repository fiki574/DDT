version: "2"

services:
  liberty:
    image: websphere-liberty:latest
    restart: always
    environment:
      LICENSE: accept
    ports:
      - 4410:9080
      - 4411:9443
    volumes:
      - "wlp:/opt/ibm/wlp"

volumes:
  wlp: {}
